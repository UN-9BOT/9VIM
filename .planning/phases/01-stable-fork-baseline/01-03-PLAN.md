---
phase: 01-stable-fork-baseline
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - 8vim/src/main/kotlin/inc/flide/vim8/ime/keyboard/text/KeyboardManager.kt
  - 8vim/src/test/kotlin/inc/flide/vim8/ime/keyboard/text/KeyboardManagerSpec.kt
files_created:
  - docs/compatibility/enter-newline.md
autonomous: true
requirements:
  - REQ-stable-fork-baseline
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-10/D-13: Enter behavior is fixed by existing JVM specs plus an honest compatibility note, not claimed as device/app proof."
    - "D-11: IME_FLAG_NO_ENTER_ACTION always wins over an explicit editor action."
    - "D-12: exactly the four representative behavior families are covered without a Cartesian fixture layer."
  artifacts:
    - 8vim/src/test/kotlin/inc/flide/vim8/ime/keyboard/text/KeyboardManagerSpec.kt
    - docs/compatibility/enter-newline.md
  key_links:
    - "KeyboardManager.onInputKeyUp -> handleEnter -> EditorInstance.performEnter/performEnterAction"
---

<objective>
Freeze the Enter/newline precedence contract at the public keyboard-routing seam.

Purpose: make the messaging-field regression visible before multilingual work changes input state.
Output: four representative JVM regression cases, any minimal runtime correction they prove necessary, and a scope-accurate compatibility note.
</objective>

<execution_context>
@.planning/phases/01-stable-fork-baseline/01-CONTEXT.md
@.planning/phases/01-stable-fork-baseline/01-RESEARCH.md
@.planning/phases/01-stable-fork-baseline/01-PATTERNS.md
@.planning/phases/01-stable-fork-baseline/01-VALIDATION.md
</execution_context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove Enter routing through onInputKeyUp with four locked cases</name>
  <read_first>
    - 8vim/src/main/kotlin/inc/flide/vim8/ime/keyboard/text/KeyboardManager.kt
    - 8vim/src/main/kotlin/inc/flide/vim8/ime/editor/ImeOptions.kt
    - 8vim/src/main/kotlin/inc/flide/vim8/ime/editor/EditorInstance.kt
    - 8vim/src/test/kotlin/inc/flide/vim8/ime/keyboard/text/KeyboardManagerSpec.kt
    - 8vim/src/test/kotlin/inc/flide/vim8/ime/editor/EditorInstanceSpec.kt
  </read_first>
  <files>8vim/src/test/kotlin/inc/flide/vim8/ime/keyboard/text/KeyboardManagerSpec.kt, 8vim/src/main/kotlin/inc/flide/vim8/ime/keyboard/text/KeyboardManager.kt</files>
  <behavior>
    - Multiline NONE routes to performEnter/newline.
    - Multiline with NO_ENTER_ACTION routes to performEnter.
    - Single-line SEND and DONE route to performEnterAction when the flag is absent.
    - Every explicit SEND/DONE/GO/SEARCH/NEXT/PREVIOUS action routes to performEnter when NO_ENTER_ACTION is present.
  </behavior>
  <action>Per D-10/D-11/D-12 add named tests directly to KeyboardManagerSpec through public `onInputKeyUp`, using the existing MockK setup and Kotest parameterization only for the explicit-action family. Run the added cases before touching runtime code. If current handleEnter already passes, keep runtime unchanged and commit regression evidence; if a case fails, make the smallest production correction after RED. Do not add an app-name allowlist, a new fixture abstraction, instrumentation, or a full InputType/action/flag product.</action>
  <acceptance_criteria>The four representative families prove the precedence contract, and tests verify the negative call as well as the selected call.</acceptance_criteria>
  <verify>
    <automated>./gradlew :8vim:testDebugUnitTest --tests 'inc.flide.vim8.ime.keyboard.text.KeyboardManagerSpec' --tests 'inc.flide.vim8.ime.editor.EditorInstanceSpec'</automated>
    <fails_when>A representative case routes to the wrong EditorInstance method, both methods are called, or the JVM spec requires a new fixture layer.</fails_when>
  </verify>
  <done>Enter/newline behavior is regression-visible at the actual key-up routing boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Record the JVM contract and deferred device/app boundary</name>
  <read_first>
    - .planning/phases/01-stable-fork-baseline/01-CONTEXT.md
    - 8vim/src/test/kotlin/inc/flide/vim8/ime/keyboard/text/KeyboardManagerSpec.kt
    - 8vim/src/test/kotlin/inc/flide/vim8/ime/editor/EditorInstanceSpec.kt
  </read_first>
  <files>docs/compatibility/enter-newline.md</files>
  <action>Per D-10/D-13 document the precedence rule, the exact four representative families, the JVM command, and the division between KeyboardManager routing and EditorInstance platform delegation. State explicitly that the contract is fixed in JVM regression tests and that Slack/Discord/current-device compatibility and the full app/input matrix are deferred; do not claim device evidence.</action>
  <acceptance_criteria>A reviewer can reproduce the contract from the note and cannot mistake JVM coverage for Android application compatibility.</acceptance_criteria>
  <verify>
    <automated>rg -q 'IME_FLAG_NO_ENTER_ACTION' docs/compatibility/enter-newline.md &amp;&amp; rg -qi 'multiline' docs/compatibility/enter-newline.md &amp;&amp; rg -q 'SEND' docs/compatibility/enter-newline.md &amp;&amp; rg -q 'DONE' docs/compatibility/enter-newline.md &amp;&amp; rg -q 'GO' docs/compatibility/enter-newline.md &amp;&amp; rg -q 'SEARCH' docs/compatibility/enter-newline.md &amp;&amp; rg -q 'NEXT' docs/compatibility/enter-newline.md &amp;&amp; rg -q 'PREVIOUS' docs/compatibility/enter-newline.md &amp;&amp; rg -qi 'JVM' docs/compatibility/enter-newline.md &amp;&amp; rg -qi 'defer' docs/compatibility/enter-newline.md &amp;&amp; ! rg -qi '(Slack|Discord|device).{0,40}(verified|passed|supported)' docs/compatibility/enter-newline.md</automated>
    <fails_when>Any precedence/action/case/JVM/deferred evidence class is absent, or the note claims Slack, Discord, or device verification.</fails_when>
  </verify>
  <done>The regression evidence and its limitation are durable and explicit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| EditorInfo metadata → IME routing | An editor controls flags/actions that decide whether text or an action is emitted. |

## STRIDE Threat Register — ASVS Level 1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-ROUTE | Tampering | KeyboardManager.handleEnter | medium | mitigate | Explicit precedence tests verify flag wins and exactly one public EditorInstance path is called. |
| T-03-CLAIM | Repudiation | compatibility note | low | accept | Record exact JVM evidence and explicitly defer device/app claims. |
</threat_model>

## Artifacts this phase produces

- Four representative Enter/newline JVM regression families.
- `docs/compatibility/enter-newline.md` with exact contract and scope boundary.

<verification>
Run the KeyboardManager and EditorInstance JVM specs; no connected test is required by this plan.
</verification>

<success_criteria>
The flag/action precedence is executable documentation and the project makes no unsupported device compatibility claim.
</success_criteria>

<output>Create `.planning/phases/01-stable-fork-baseline/01-03-SUMMARY.md` when complete.</output>
