---
phase: 01-stable-fork-baseline
plan: 07
type: execute
wave: 5
depends_on:
  - 01-06
files_modified: []
files_created:
  - refs/tags/fork-baseline-v0.18.0-rc.1
autonomous: true
requirements:
  - REQ-stable-fork-baseline
estimate:
  tokens: 9000
  raw_tokens: 9000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-17: one annotated immutable tag is created only for the exact SHA approved in Plan 06."
    - "Planning metadata commits may follow proof, but no source/build/runtime/docs file may change after approved_sha."
    - "The tag name is absent locally and remotely immediately before creation, then origin resolves its peeled target to approved_sha rather than current HEAD."
  artifacts:
    - refs/tags/fork-baseline-v0.18.0-rc.1
    - .planning/phases/01-stable-fork-baseline/01-07-SUMMARY.md
  key_links:
    - "01-06-SUMMARY approved_sha -> validated commit object -> local annotated tag target -> origin peeled tag target"
---

<objective>
Create and publish the single immutable baseline marker after proof approval.

Purpose: provide a shared regression-comparison identity without moving or overwriting any tag.
Output: annotated local and origin tag `fork-baseline-v0.18.0-rc.1` at the approved SHA.
</objective>

<execution_context>
@.planning/phases/01-stable-fork-baseline/01-CONTEXT.md
@.planning/phases/01-stable-fork-baseline/01-06-PLAN.md
@.planning/phases/01-stable-fork-baseline/01-06-SUMMARY.md
@docs/compatibility/baseline.md
</execution_context>

<tasks>

<task type="tracer">
  <name>Task 1: Recheck absence and create the annotated local tag</name>
  <precondition>01-06-SUMMARY contains exactly one `approved_sha=&lt;40-hex commit&gt;` with approved proof, picker pass, settled #622 outcome, and local/origin tag absence; every commit after approved_sha changes only `.planning/` metadata.</precondition>
  <reversibility rating="costly">Replacing D-17's published comparison marker would require coordinated provenance repair.</reversibility>
  <read_first>
    - .planning/phases/01-stable-fork-baseline/01-06-SUMMARY.md
    - docs/compatibility/baseline.md
    - 8vim/version.properties
  </read_first>
  <files>git ref refs/tags/fork-baseline-v0.18.0-rc.1</files>
  <action>Parse `approved_sha` only from the machine-readable 01-06-SUMMARY field, require exactly 40 lowercase hexadecimal characters, validate it resolves to a commit object, and require it is an ancestor of current HEAD. Compare approved_sha..HEAD paths and halt if anything outside `.planning/` changed after proof; planning summaries/state/roadmap commits are allowed because they cannot change the proven product tree. Immediately recheck that the exact tag name is absent in local refs and `origin`. Per D-17 create one annotated tag explicitly on approved_sha, not HEAD; its message includes proof branch, approved_sha, evidence URL/output reference, picker pass, and #622 outcome. Never use force, move, delete, overwrite, or reuse an existing tag.</action>
  <acceptance_criteria>One annotated local tag exists at the validated approved_sha with evidence-bearing annotation; post-proof diffs contain only `.planning/` paths, and any invalid SHA, source/build/runtime/docs change, or pre-existing local/remote tag halts without mutation.</acceptance_criteria>
  <verify>
    <automated>approved_sha="$(sed -n 's/^approved_sha=\([0-9a-f]\{40\}\)$/\1/p' .planning/phases/01-stable-fork-baseline/01-06-SUMMARY.md)" &amp;&amp; test "${#approved_sha}" -eq 40 &amp;&amp; test "$(git cat-file -t "$approved_sha")" = commit &amp;&amp; git merge-base --is-ancestor "$approved_sha" HEAD &amp;&amp; changed="$(git diff --name-only "$approved_sha"..HEAD)" &amp;&amp; unexpected="$(printf '%s\n' "$changed" | sed '/^\.planning\//d; /^$/d')" &amp;&amp; test -z "$unexpected" &amp;&amp; test "$(git tag --list 'fork-baseline-v0.18.0-rc.1')" = 'fork-baseline-v0.18.0-rc.1' &amp;&amp; test "$(git rev-list -n 1 fork-baseline-v0.18.0-rc.1)" = "$approved_sha" &amp;&amp; test "$(git cat-file -t fork-baseline-v0.18.0-rc.1)" = tag &amp;&amp; annotation="$(git tag -n99 fork-baseline-v0.18.0-rc.1)" &amp;&amp; test -n "$annotation" &amp;&amp; rg -q 'proof=' &lt;&lt;&lt; "$annotation" &amp;&amp; rg -q "approved_sha=$approved_sha" &lt;&lt;&lt; "$annotation"</automated>
    <fails_when>approved_sha is missing/ambiguous/invalid, is not an ancestor commit, any post-proof path lies outside `.planning/`, the tag is absent/lightweight, points anywhere except approved_sha, lacks proof/approved_sha annotation, or creation overwrote an existing ref.</fails_when>
  </verify>
  <done>The proven product commit—not a later planning-only HEAD—has one local annotated immutable marker.</done>
</task>

<task type="auto">
  <name>Task 2: Publish the exact tag ref and verify remote immutability evidence</name>
  <precondition>The local annotated tag exists at 01-06-SUMMARY approved_sha and origin did not contain that tag during Task 1's immediate pre-create check.</precondition>
  <read_first>
    - .planning/phases/01-stable-fork-baseline/01-06-SUMMARY.md
  </read_first>
  <files>remote ref refs/tags/fork-baseline-v0.18.0-rc.1, 01-07-SUMMARY.md evidence</files>
  <action>Parse and validate the same approved_sha again, recheck origin absence immediately before publication, then push only the exact annotated tag ref to `origin` without force. Query both the remote tag object and peeled target, require the peeled target to equal approved_sha rather than HEAD, and record approved_sha, planning-only HEAD, local tag object, remote tag object, peeled SHA, UTC time, and push result in 01-07-SUMMARY. Never push another branch/ref, overwrite, delete, or force-update the tag.</action>
  <acceptance_criteria>Origin exposes exactly the intended annotated tag and its peeled target equals the approved SHA; summary evidence is sufficient to detect later movement.</acceptance_criteria>
  <verify>
    <automated>approved_sha="$(sed -n 's/^approved_sha=\([0-9a-f]\{40\}\)$/\1/p' .planning/phases/01-stable-fork-baseline/01-06-SUMMARY.md)" &amp;&amp; test "${#approved_sha}" -eq 40 &amp;&amp; test "$(git rev-list -n 1 fork-baseline-v0.18.0-rc.1)" = "$approved_sha" &amp;&amp; git push origin refs/tags/fork-baseline-v0.18.0-rc.1 &amp;&amp; remote_tag_line="$(git ls-remote --tags origin refs/tags/fork-baseline-v0.18.0-rc.1)" &amp;&amp; test -n "$remote_tag_line" &amp;&amp; peeled_line="$(git ls-remote --tags origin 'refs/tags/fork-baseline-v0.18.0-rc.1^{}')" &amp;&amp; peeled_sha="${peeled_line%%$'\t'*}" &amp;&amp; test "$peeled_sha" = "$approved_sha"</automated>
    <fails_when>approved_sha is invalid, the local tag target differs from it, push is rejected, remote tag/object is absent, remote peeled target differs from approved_sha, or overwrite/force/another ref would be used.</fails_when>
  </verify>
  <done>The shared repository has one immutable annotated baseline tag with recorded remote proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Approved proof summary → local tag | Stale or mismatched SHA could receive the marker. |
| Local tag → origin refs | Force/reuse could rewrite shared provenance. |

## STRIDE Threat Register — ASVS Level 1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-SHA | Spoofing | tag target | high | mitigate | Parse/validate approved_sha as a commit, allow only `.planning/` changes afterward, and target that commit explicitly rather than HEAD. |
| T-07-REF | Tampering | local/origin tag refs | high | mitigate | Check absence twice, use annotated non-force creation/push, verify peeled remote target. |
| T-07-AUDIT | Repudiation | remote publication | medium | mitigate | Record object IDs, peeled SHA, UTC time, and push result in SUMMARY. |
</threat_model>

## Artifacts this phase produces

- Local and origin annotated `fork-baseline-v0.18.0-rc.1` tag.
- `01-07-SUMMARY.md` with approved_sha, planning-only HEAD, and immutable remote-ref evidence.

## Proof artifacts consumed

- `01-06-SUMMARY.md` machine-readable `approved_sha` is the sole tag-target authority.
- `docs/compatibility/baseline.md` supplies stable provenance included before proof.

<verification>
Both local and remote tag object checks must resolve to the Plan 06 approved SHA; no force operation is permitted.
</verification>

<success_criteria>
The exact proven baseline commit is reachable through one shared immutable annotated tag.
</success_criteria>

<output>Create `.planning/phases/01-stable-fork-baseline/01-07-SUMMARY.md` when complete.</output>
