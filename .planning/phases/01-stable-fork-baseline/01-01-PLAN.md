---
phase: 01-stable-fork-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/pr-test.yml
  - 8vim/build.gradle.kts
files_created:
  - scripts/baseline-check.sh
  - config/baseline-toolchain.properties
autonomous: true
requirements:
  - REQ-stable-fork-baseline
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-01/D-02: one visible repository command fails when any unit, lint, ktlint, checkstyle, or debug-APK gate fails."
    - "D-04/D-05: Java 17, Gradle wrapper, and Android API 36 expectations are explicit, and every pull request runs the same command without a path filter."
    - "D-03: connected Android tests remain a separate smoke activity, not part of the mandatory command."
    - "D-14/D-18: PR #604 reproducible metadata behavior is ported without dependency modernization or historic Dependabot changes."
  artifacts:
    - scripts/baseline-check.sh
    - config/baseline-toolchain.properties
    - .github/workflows/pr-test.yml
    - 8vim/build.gradle.kts
  key_links:
    - ".github/workflows/pr-test.yml -> scripts/baseline-check.sh"
    - "scripts/baseline-check.sh -> config/baseline-toolchain.properties"
    - "scripts/baseline-check.sh -> Gradle wrapper module tasks"
---

<objective>
Create the reproducible baseline tracer: the same repository-owned command is inspectable locally and is the unconditional pull-request gate.

Purpose: prove the build/test path before feature changes and expose toolchain mismatch honestly.
Output: baseline script, pinned toolchain manifest, all-PR workflow wiring, and deterministic AboutLibraries metadata.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stable-fork-baseline/01-CONTEXT.md
@.planning/phases/01-stable-fork-baseline/01-RESEARCH.md
@.planning/phases/01-stable-fork-baseline/01-PATTERNS.md
@.planning/phases/01-stable-fork-baseline/01-VALIDATION.md
</execution_context>

<coverage_note>
The spec-less probe classified REQ-stable-fork-baseline as unresolved/unclassified. This plan closes the build portion with an executable script contract, unconditional CI invocation, explicit failure directions, and a later final-tag proof rather than treating the probe as implicit coverage.
</coverage_note>

<tasks>

<task type="tracer">
  <name>Task 1: Wire one manifest-validated baseline command through the real Gradle gates</name>
  <read_first>
    - gradle/wrapper/gradle-wrapper.properties
    - 8vim/build.gradle.kts
    - gradle/libs.versions.toml
    - .github/workflows/codecheck.yaml
    - .github/workflows/build.yaml
  </read_first>
  <files>scripts/baseline-check.sh, config/baseline-toolchain.properties</files>
  <action>Create a POSIX-compatible executable script using strict failure semantics. Per D-01/D-02/D-04 it must read every flat key=value expectation for Gradle 9.4.0, AGP 9.1.0, Java 17, compile SDK 36, target SDK 35, min SDK 24, and build tools 36.0.0, then invoke the tracked wrapper once with `--no-daemon :8vim:testDebugUnitTest :8vim:lint :8vim:ktlintCheck :8vim:checkstyle :8vim:assembleDebug`. Provide a documented `--preflight-only` diagnostic and `BASELINE_TOOLCHAIN_MANIFEST` override so developers/CI can validate an alternate manifest without starting Gradle; both are production operator interfaces, not hidden test seams. Validate prerequisites without installing or mutating the host. Keep connected tests out per D-03. Make errors name the observed and expected toolchain value so the known local Java 26/missing Platform 36 state is reported as environment mismatch, never as a green baseline.</action>
  <acceptance_criteria>Each of seven manifest entries is independently asserted, the script visibly reads that manifest, and a deliberately incompatible manifest makes `--preflight-only` exit non-zero before Gradle.</acceptance_criteria>
  <verify>
    <automated>bash -n scripts/baseline-check.sh &amp;&amp; test -x scripts/baseline-check.sh &amp;&amp; for kv in 'gradle=9.4.0' 'agp=9.1.0' 'java=17' 'compileSdk=36' 'targetSdk=35' 'minSdk=24' 'buildTools=36.0.0'; do rg -Fx "$kv" config/baseline-toolchain.properties &gt;/dev/null || exit 1; done &amp;&amp; rg -q 'baseline-toolchain\.properties' scripts/baseline-check.sh &amp;&amp; rg -q 'BASELINE_TOOLCHAIN_MANIFEST' scripts/baseline-check.sh &amp;&amp; rg -q -- '--preflight-only' scripts/baseline-check.sh &amp;&amp; rg -n ':8vim:testDebugUnitTest.*:8vim:lint.*:8vim:ktlintCheck.*:8vim:checkstyle.*:8vim:assembleDebug' scripts/baseline-check.sh &amp;&amp; mismatch_manifest="$(mktemp)" &amp;&amp; cp config/baseline-toolchain.properties "$mismatch_manifest" &amp;&amp; sed -i 's/^java=17$/java=99/' "$mismatch_manifest" &amp;&amp; ! BASELINE_TOOLCHAIN_MANIFEST="$mismatch_manifest" ./scripts/baseline-check.sh --preflight-only; status=$?; rm -f "$mismatch_manifest"; exit $status</automated>
    <fails_when>Any individual key/value is absent, the script does not consume the manifest/operator interfaces, a mismatched manifest passes preflight, or any mandatory Gradle task is missing from the single invocation.</fails_when>
  </verify>
  <done>The repository has one honest, runnable baseline entrypoint with observable prerequisite and gate failures.</done>
</task>

<task type="auto">
  <name>Task 2: Make the baseline command unconditional on every pull request</name>
  <read_first>
    - .github/workflows/pr-test.yml
    - .github/workflows/codecheck.yaml
    - .github/workflows/build.yaml
    - scripts/baseline-check.sh
    - config/baseline-toolchain.properties
  </read_first>
  <files>.github/workflows/pr-test.yml</files>
  <action>Per D-05 replace the path-filter-dependent mandatory test/build topology with an unconditional PR job that checks out the repository, provisions Temurin Java 17 and Android SDK/API 36 consistent with the manifest, sets up Gradle, and invokes only `./scripts/baseline-check.sh`. It may retain YAML validation as an additional conditional job, but the baseline job must not depend on `dorny/paths-filter`, `changes` outputs, labels, or changed paths. Do not duplicate the Gradle task list in YAML.</action>
  <acceptance_criteria>Top-level pull_request has no path filters; the mandatory `baseline` job has no `if`/`needs`/changes filter and the entire workflow invokes the repository script exactly once.</acceptance_criteria>
  <verify>
    <automated>python3 -c 'import yaml; d=yaml.safe_load(open(".github/workflows/pr-test.yml")); trigger=d.get("on", d.get(True, {})); pr=trigger["pull_request"]; assert not any(k in pr for k in ("paths", "paths-ignore")); jobs=d["jobs"]; baseline=jobs["baseline"]; assert "if" not in baseline and "needs" not in baseline; encoded=str(baseline).lower(); assert "paths-filter" not in encoded and "changes" not in encoded; runs=[step.get("run", "") for job in jobs.values() if isinstance(job, dict) for step in job.get("steps", []) if isinstance(step, dict)]; assert sum("./scripts/baseline-check.sh" in run for run in runs)==1'</automated>
    <fails_when>YAML parsing fails, pull_request has paths/paths-ignore, the mandatory baseline job has any conditional dependency/path-change gate, or script invocation count is not exactly one.</fails_when>
  </verify>
  <done>The mandatory product gate runs for every PR and executes the repository command unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Port the reproducible AboutLibraries metadata fix only</name>
  <read_first>
    - 8vim/build.gradle.kts
    - gradle/libs.versions.toml
    - .planning/phases/01-stable-fork-baseline/01-RESEARCH.md
  </read_first>
  <files>8vim/build.gradle.kts</files>
  <action>Per D-14 add the minimal PR #604 behavior `aboutLibraries { excludeFields = arrayOf("generated") }` beside existing plugin/build configuration. Per D-18 do not change dependency versions, cherry-pick historic Dependabot work, or modernize the build. Preserve current SDK and Java settings.</action>
  <acceptance_criteria>Generated timestamps are excluded from metadata while the version catalog and dependency graph remain unchanged.</acceptance_criteria>
  <verify>
    <automated>rg -U 'aboutLibraries\s*\{[\s\S]*excludeFields\s*=\s*arrayOf\("generated"\)' 8vim/build.gradle.kts &amp;&amp; git diff --exit-code -- gradle/libs.versions.toml gradle/wrapper/gradle-wrapper.properties</automated>
    <fails_when>The exclusion is absent or dependency/wrapper versions changed as part of this task.</fails_when>
  </verify>
  <done>Mandatory upstream #604 behavior is isolated and reproducible without dependency churn.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Pull request → CI runner | Untrusted changes attempt to bypass mandatory checks. |
| Runner toolchain → Gradle build | Host Java/SDK versions can produce non-reproducible or misleading results. |

## STRIDE Threat Register — ASVS Level 1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-CI | Tampering | pr-test.yml | high | mitigate | Unconditional baseline job delegates to one tracked script; static verification rejects path-conditioned wiring. |
| T-01-TOOLCHAIN | Spoofing | baseline-check.sh | high | mitigate | Compare observed Java/wrapper/SDK values with manifest and fail before build on mismatch. |
| T-01-SUPPLY | Tampering | Gradle/dependencies | medium | mitigate | Use existing wrapper/version catalog and prohibit dependency updates in this plan. |
</threat_model>

## Artifacts this phase produces

- `scripts/baseline-check.sh` — canonical full baseline proof command.
- `config/baseline-toolchain.properties` — explicit wrapper/JDK/SDK contract.
- `.github/workflows/pr-test.yml` — unconditional pull-request entrypoint.
- `8vim/build.gradle.kts` — PR #604 deterministic metadata configuration.

<verification>
The structural checks above must pass on any host. The product proof is `./scripts/baseline-check.sh`; on the currently observed host it is expected to fail at preflight until Java 17 and Android Platform 36 exist, while CI must provide the green proof at the final plan.
</verification>

<success_criteria>
One documented command covers every mandatory automated baseline gate and every PR runs it with the declared toolchain.
</success_criteria>

<output>Create `.planning/phases/01-stable-fork-baseline/01-01-SUMMARY.md` when complete.</output>
