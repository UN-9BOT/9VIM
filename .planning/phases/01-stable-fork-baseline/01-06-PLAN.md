---
phase: 01-stable-fork-baseline
plan: 06
type: execute
wave: 4
depends_on:
  - 01-01
  - 01-02
  - 01-03
  - 01-04
  - 01-05
files_modified:
  - docs/compatibility/baseline.md
files_created: []
autonomous: false
requirements:
  - REQ-stable-fork-baseline
estimate:
  tokens: 15000
  raw_tokens: 15000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The exact manifest-compatible baseline command is green for the final commit; the known local Java/SDK gap is never reported as project success."
    - "D-17: exact-SHA proof is human-approved before Plan 07 is allowed to create the immutable tag."
    - "D-03/D-10/D-15: connected tests, Enter device matrix, and omitted #622 work are not silently promoted into mandatory automated evidence."
  artifacts:
    - docs/compatibility/baseline.md
    - .planning/phases/01-stable-fork-baseline/01-06-SUMMARY.md
  key_links:
    - "final commit SHA -> local or GitHub-CI proof -> blocking approval -> Plan 07 tag precondition"
---

<objective>
Close the evidence ledger and approve one honest exact-SHA proof branch before tagging.

Purpose: prevent unavailable local tooling or a different CI SHA from being treated as final proof.
Output: final evidence ledger and structured local/remote proof approval consumed by Plan 07.
</objective>

<execution_context>
@.planning/phases/01-stable-fork-baseline/01-CONTEXT.md
@.planning/phases/01-stable-fork-baseline/01-VALIDATION.md
@.planning/phases/01-stable-fork-baseline/01-01-SUMMARY.md
@.planning/phases/01-stable-fork-baseline/01-02-SUMMARY.md
@.planning/phases/01-stable-fork-baseline/01-03-SUMMARY.md
@.planning/phases/01-stable-fork-baseline/01-04-SUMMARY.md
@.planning/phases/01-stable-fork-baseline/01-05-SUMMARY.md
@docs/compatibility/baseline.md
</execution_context>

<tasks>

<task type="tracer">
  <name>Task 1: Finalize the evidence ledger before the proof commit</name>
  <read_first>
    - scripts/baseline-check.sh
    - config/baseline-toolchain.properties
    - .github/workflows/pr-test.yml
    - docs/compatibility/baseline.md
    - .planning/phases/01-stable-fork-baseline/01-VALIDATION.md
  </read_first>
  <files>docs/compatibility/baseline.md</files>
  <action>Complete baseline.md from Plans 01-05 before final proof: include the canonical command/toolchain, targeted JVM command, mandatory picker device/API/build evidence, #604/#614 provenance, Enter JVM-only scope, exact #622 outcome, excluded #553/Dependabot work, the known local Java 26/missing Platform 36 limitation, and intended tag name. Do not insert a fabricated final SHA or green result; the final exact-SHA CI URL/result belongs in the annotated tag message and 01-06-SUMMARY after this documentation commit exists.</action>
  <acceptance_criteria>The ledger contains every stable evidence item and limitation needed before the final proof, while run-specific proof fields remain honest and are assigned to the tag annotation/SUMMARY.</acceptance_criteria>
  <verify>
    <automated>rg -q 'baseline-check\.sh' docs/compatibility/baseline.md &amp;&amp; rg -q 'Java 17' docs/compatibility/baseline.md &amp;&amp; rg -q 'API 36' docs/compatibility/baseline.md &amp;&amp; rg -q '#604' docs/compatibility/baseline.md &amp;&amp; rg -q '#614' docs/compatibility/baseline.md &amp;&amp; rg -qi 'custom' docs/compatibility/baseline.md &amp;&amp; rg -q 'URI' docs/compatibility/baseline.md &amp;&amp; rg -q 'Enter' docs/compatibility/baseline.md &amp;&amp; rg -q 'JVM' docs/compatibility/baseline.md &amp;&amp; rg -q '#622' docs/compatibility/baseline.md &amp;&amp; rg -q '#553' docs/compatibility/baseline.md &amp;&amp; rg -q 'Dependabot' docs/compatibility/baseline.md &amp;&amp; rg -q 'Java 26' docs/compatibility/baseline.md &amp;&amp; rg -q 'fork-baseline-v0\.18\.0-rc\.1' docs/compatibility/baseline.md &amp;&amp; ! rg -qi 'final (CI|baseline).*(green|passed)|tag (created|pushed)' docs/compatibility/baseline.md</automated>
    <fails_when>Any independent toolchain/upstream/custom/Enter/exclusion/local-limitation/tag-intent class is absent, or the ledger claims final green CI/tag creation before approval.</fails_when>
  </verify>
  <done>The next commit can be tested and tagged without any further tracked-file mutation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Approve either local-toolchain proof or exact-SHA GitHub-CI proof</name>
  <read_first>
    - docs/compatibility/baseline.md
    - scripts/baseline-check.sh
    - config/baseline-toolchain.properties
    - .github/workflows/pr-test.yml
    - .planning/phases/01-stable-fork-baseline/01-VALIDATION.md
  </read_first>
  <files>None; record the approved branch and evidence in 01-06-SUMMARY.md.</files>
  <action>Select exactly one proof branch for the committed Task 1 HEAD. Local branch: first prove Java 17 and Android Platform/Build Tools 36 match the manifest, then run the baseline script and targeted layout/Enter JVM command and preserve exit/output evidence. Remote branch: do not run unavailable local tools; trigger/inspect the unconditional GitHub `baseline` job, prove it invoked `./scripts/baseline-check.sh`, completed successfully for the exact HEAD SHA, and preserve its run URL. For both branches, confirm picker pass/#622 outcome from prior summaries and prove `fork-baseline-v0.18.0-rc.1` is absent both in local refs and `origin` tag refs. Do not create or approve the tag in this checkpoint.</action>
  <acceptance_criteria>Approval writes a machine-readable `approved_sha=&lt;40-hex commit&gt;` line plus `proof=local|ci`, toolchain or GitHub run evidence, targeted/full results, picker/#622 outcomes, and `tag_absent_local=true; tag_absent_origin=true`. Any mismatch, missing run URL/output, local toolchain gap on the local branch, existing tag, or non-green command blocks approval.</acceptance_criteria>
  <verify>
    <human-check>For `proof=local`, inspect manifest-matching toolchain plus successful full/targeted output for exact HEAD. For `proof=ci`, inspect the successful mandatory baseline job URL and exact HEAD SHA without requiring local Java/SDK. In both cases inspect local `git tag --list` and remote `git ls-remote --tags origin` results showing the intended tag absent.</human-check>
  </verify>
  <what-built>A committed, evidence-complete baseline candidate with two explicit proof routes and no tag yet.</what-built>
  <how-to-verify>Reject if neither proof branch is complete, if evidence targets a different SHA, if picker is not pass, if #622 is unsettled, or if the tag exists locally/remotely. Approve exactly one proof branch and preserve the structured fields.</how-to-verify>
  <resume-signal>Reply `proof=local|ci; approved_sha=&lt;40-hex commit&gt;; evidence=&lt;output-or-run-url&gt;; picker=pass; #622=&lt;outcome&gt;; tag_absent_local=true; tag_absent_origin=true`.</resume-signal>
  <done>Plan 07 receives an exact-SHA proof approval without nested or automatic approval behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| CI/local proof → provenance ledger | A result from another SHA/toolchain could be misattributed to the baseline. |
| Local/remote proof → tag authorization | Only exact-SHA evidence may authorize Plan 07. |

## STRIDE Threat Register — ASVS Level 1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-PROOF | Spoofing | baseline evidence | high | mitigate | Match green job/toolchain to exact final SHA and preserve command/run URL. |
| T-06-TAG | Tampering | proof checkpoint | high | mitigate | Verify intended tag absent locally/remotely and defer creation to dependent Plan 07. |
| T-06-SCOPE | Repudiation | deferred/optional checks | medium | mitigate | Ledger distinguishes JVM, device, optional omission, and connected-test scope. |
</threat_model>

## Artifacts this phase produces

- Completed `docs/compatibility/baseline.md` evidence ledger.
- Structured exact-SHA local or GitHub-CI proof approval in `01-06-SUMMARY.md`.

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Reproducible, regression-visible baseline | 01-01..01-07 | COVERED | Command, regressions, device evidence, provenance, tag. |
| REQ | REQ-stable-fork-baseline | CI gates, upstream fixes, custom override, Enter regression, tag | 01-01..01-07 | COVERED | Spec-less unresolved probe is closed by explicit automated/manual evidence. |
| RESEARCH | — | Repository command/toolchain/all-PR CI | 01-01 | COVERED | No external API integration; Android SDK remains platform tooling. |
| RESEARCH | — | Transactional URI lifecycle/cache/fallback | 01-02, 01-04 | COVERED | Includes provider/race/stale cases and picker. |
| RESEARCH | — | Enter precedence and scope note | 01-03 | COVERED | JVM-only per locked scope. |
| RESEARCH | — | #614/#604 mandatory, #622 conditional, final provenance/tag | 01-01, 01-04..01-07 | COVERED | #553/Dependabot excluded. |
| CONTEXT | D-01–D-05 | CI contract | 01-01 | COVERED | |
| CONTEXT | D-06–D-09 | Custom-layout contract | 01-02, 01-04 | COVERED | |
| CONTEXT | D-10–D-13 | Enter/newline contract | 01-03 | COVERED | |
| CONTEXT | D-14–D-18 | Upstream and baseline marker | 01-01, 01-04..01-07 | COVERED | |

<verification>
This plan completes only when one exact-SHA proof branch is approved, picker evidence passes, #622 is settled, and the intended tag is absent locally/remotely for Plan 07.
</verification>

<success_criteria>
Developers have one reproducible command and an approved exact-SHA baseline candidate ready for the immutable tag; users have verified picker behavior and protected Enter/custom-layout semantics.
</success_criteria>

<output>Create `.planning/phases/01-stable-fork-baseline/01-06-SUMMARY.md` when complete.</output>
