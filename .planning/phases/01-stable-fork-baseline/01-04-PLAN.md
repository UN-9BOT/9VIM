---
phase: 01-stable-fork-baseline
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - 8vim/src/main/kotlin/inc/flide/vim8/app/settings/LayoutScreen.kt
files_created:
  - 8vim/src/main/kotlin/inc/flide/vim8/app/settings/CustomLayoutImportAdapter.kt
  - 8vim/src/test/kotlin/inc/flide/vim8/app/settings/CustomLayoutImportAdapterSpec.kt
autonomous: false
requirements:
  - REQ-stable-fork-baseline
estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-06/D-09/D-14: the real SAF picker accepts provider files through */*, delegates to the transactional domain operation, activates valid layouts, and alerts without mutation on failure."
    - "The custom-layout picker is manually proven on a current Android device; this evidence is not inferred from JVM tests."
    - "D-15: #622 receives an explicit current-device adopt/omit outcome before any runtime change is planned for execution."
  artifacts:
    - 8vim/src/main/kotlin/inc/flide/vim8/app/settings/LayoutScreen.kt
    - 8vim/src/main/kotlin/inc/flide/vim8/app/settings/CustomLayoutImportAdapter.kt
    - 8vim/src/test/kotlin/inc/flide/vim8/app/settings/CustomLayoutImportAdapterSpec.kt
    - .planning/phases/01-stable-fork-baseline/01-04-SUMMARY.md
  key_links:
    - "OpenDocument URI -> persistable read grant -> AvailableLayouts typed import result -> existing alert/current UI"
    - "device smoke outcome -> Plan 05 conditional #622 branch"
---

<objective>
Connect the transactional layout behavior to Android SAF and collect the two device-only decisions honestly.

Purpose: satisfy the real picker acceptance boundary and gate optional #622 before adoption.
Output: minimal #614 picker change plus durable pass/fail/unavailable checkpoint outcomes.
</objective>

<execution_context>
@.planning/phases/01-stable-fork-baseline/01-CONTEXT.md
@.planning/phases/01-stable-fork-baseline/01-RESEARCH.md
@.planning/phases/01-stable-fork-baseline/01-PATTERNS.md
@.planning/phases/01-stable-fork-baseline/01-VALIDATION.md
@.planning/phases/01-stable-fork-baseline/01-02-SUMMARY.md
</execution_context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Connect OpenDocument through a production SAF import adapter</name>
  <read_first>
    - 8vim/src/main/kotlin/inc/flide/vim8/app/settings/LayoutScreen.kt
    - 8vim/src/main/kotlin/inc/flide/vim8/ime/layout/AvailableLayouts.kt
    - 8vim/src/test/kotlin/inc/flide/vim8/ime/layout/AvailableLayoutsSpec.kt
    - .planning/phases/01-stable-fork-baseline/01-02-SUMMARY.md
  </read_first>
  <files>8vim/src/main/kotlin/inc/flide/vim8/app/settings/CustomLayoutImportAdapter.kt, 8vim/src/main/kotlin/inc/flide/vim8/app/settings/LayoutScreen.kt, 8vim/src/test/kotlin/inc/flide/vim8/app/settings/CustomLayoutImportAdapterSpec.kt</files>
  <behavior>
    - The production adapter exposes the exact OpenDocument MIME contract `*/*` from D-14.
    - A selected URI requests only persistable read permission, then calls the Plan 02 import transaction exactly once.
    - Permission/provider/domain failure returns one typed UI-facing failure and never performs a second history/current mutation.
    - LayoutScreen folds adapter success/error into active state or the existing alert and owns no duplicate validation/state logic.
  </behavior>
  <action>First add a JVM MockK spec for a production-facing `CustomLayoutImportAdapter` and observe RED. The adapter must own the stable SAF policy used outside Compose: accepted MIME types, persistable read permission acquisition, one call to AvailableLayouts' typed import, and a typed result that LayoutScreen can fold into its existing alert. This is a runtime boundary that isolates Android provider concerns from Compose, not an ad-hoc test seam. Then per D-14 implement the adapter and make LayoutScreen's OpenDocument launcher use its `*/*` MIME contract. Per D-06/D-09 valid input becomes active through the domain transaction; permission/parser/validation failure alerts without separate UI mutation. Do not duplicate YAML parsing, convert content URIs to file paths, or take PR #553/version changes.</action>
  <acceptance_criteria>The adapter tests fail before implementation, then prove MIME, permission, single delegation, and error behavior; LayoutScreen becomes thin Compose wiring over that public production boundary.</acceptance_criteria>
  <verify>
    <automated>./gradlew :8vim:testDebugUnitTest --tests 'inc.flide.vim8.app.settings.CustomLayoutImportAdapterSpec' --tests 'inc.flide.vim8.ime.layout.AvailableLayoutsSpec' --tests 'inc.flide.vim8.ime.layout.LayoutSpec'</automated>
    <fails_when>The adapter MIME/permission/delegation/error contract or domain regressions fail, or LayoutScreen bypasses the tested adapter for selected URIs.</fails_when>
  </verify>
  <done>The Android adapter uses the same validated import-to-active path proven by JVM tests.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Record current-device picker evidence and the #622 adoption gate</name>
  <read_first>
    - .planning/phases/01-stable-fork-baseline/01-VALIDATION.md
    - .planning/phases/01-stable-fork-baseline/01-RESEARCH.md
    - 8vim/src/main/kotlin/inc/flide/vim8/app/settings/LayoutScreen.kt
  </read_first>
  <files>None; record the structured outcome in 01-04-SUMMARY.md.</files>
  <action>First install the baseline debug APK on a current Android device and run the required picker smoke: valid provider YAML activates/appears, same URI does not duplicate, and invalid/zero-layer content alerts with current unchanged. For #622, never test an imaginary baseline change: create a disposable worktree/branch from the exact baseline candidate SHA, apply only the isolated upstream #622 diff there, build a candidate APK with the Phase 1 manifest toolchain, and record its candidate commit/patch identity. Install that candidate and run default-on compatibility, disabled unresolved-center reset, single commit after reset, valid center-hit, restart persistence, letters/layers/long-press. If all pass, record `#622=adopt` and retain the patch identity only as evidence for Plan 05's clean port. If unavailable/failing, record the omit outcome and discard the disposable candidate worktree/branch; no candidate commit may enter the baseline. D-15 forbids adoption without this flow.</action>
  <acceptance_criteria>The summary records baseline SHA/APK, device/API, picker pass, candidate SHA/patch identity when tested, checklist observations, and exactly one #622 outcome. Picker failure blocks Phase 1. #622 can be `adopt` only after every candidate check passes; unavailable/failure requires omission and candidate disposal.</acceptance_criteria>
  <verify>
    <human-check>Verify the baseline picker steps and record `picker=pass|fail`. Then verify that #622 was tested only from an isolated disposable candidate APK and record `#622=adopt|omit-unavailable|omit-failed`, device/API, baseline SHA, candidate SHA/patch identity, and disposal/retention-for-evidence status.</human-check>
  </verify>
  <what-built>Tested SAF custom-layout import on the baseline APK plus a reversible, isolated #622 candidate APK that is not part of baseline history.</what-built>
  <how-to-verify>Reject approval if picker evidence is missing/failing; if `#622=adopt`, require all candidate checks, exact candidate identity, and proof the candidate stayed outside baseline history. For either omit outcome, require documented unavailability/failure and disposal of the candidate worktree/branch.</how-to-verify>
  <resume-signal>Reply with `picker=pass; #622=adopt` or `picker=pass; #622=omit-unavailable|omit-failed`, plus device/API/build SHA. Describe any picker failure instead of approving.</resume-signal>
  <done>Manual evidence distinguishes required picker acceptance from the optional #622 branch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Android SAF/provider → Compose adapter | URI grant and provider behavior are untrusted platform inputs. |
| Human device evidence → optional runtime branch | An unsupported claim could incorrectly enable #622. |

## STRIDE Threat Register — ASVS Level 1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-URI | Elevation of privilege | LayoutScreen picker | high | mitigate | Request only persistable read permission and delegate untrusted bytes to validated domain import. |
| T-04-ERROR | Repudiation | picker alert/state | medium | mitigate | Device smoke records invalid/zero-layer alert and unchanged current identity. |
| T-04-EVIDENCE | Spoofing | #622 gate | high | mitigate | Blocking-human checkpoint requires current-device/API/SHA evidence before `adopt`. |
</threat_model>

## Artifacts this phase produces

- Minimal upstream #614 MIME behavior in `LayoutScreen.kt`.
- Structured current-device picker result and #622 adopt/omit decision in `01-04-SUMMARY.md`.

<verification>
Automated layout regressions precede the mandatory human picker smoke. #622 may be omitted, but picker acceptance may not.
</verification>

<success_criteria>
The real Android picker activates valid custom layouts and rejects invalid ones transactionally; optional gesture work is gated by evidence.
</success_criteria>

<output>Create `.planning/phases/01-stable-fork-baseline/01-04-SUMMARY.md` with the exact structured outcomes when complete.</output>
