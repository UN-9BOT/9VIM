---
phase: 02-multilingual-profiles-and-settings
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - 8vim/src/main/kotlin/inc/flide/vim8/AppPrefs.kt
  - 8vim/src/main/kotlin/inc/flide/vim8/VIM8Application.kt
  - 8vim/src/main/kotlin/inc/flide/vim8/Vim8ImeService.kt
  - 8vim/src/main/kotlin/inc/flide/vim8/ime/language/LanguageProfile.kt
  - 8vim/src/main/kotlin/inc/flide/vim8/ime/language/LanguageConfigSerDe.kt
  - 8vim/src/main/kotlin/inc/flide/vim8/ime/language/LanguageConfigNormalizer.kt
  - 8vim/src/main/kotlin/inc/flide/vim8/ime/language/LanguageMigration.kt
  - 8vim/src/main/kotlin/inc/flide/vim8/ime/language/LanguageManager.kt
  - 8vim/src/test/kotlin/inc/flide/vim8/ime/language/LanguageConfigSerDeSpec.kt
  - 8vim/src/test/kotlin/inc/flide/vim8/ime/language/LanguageMigrationSpec.kt
  - 8vim/src/test/kotlin/inc/flide/vim8/ime/language/LanguageManagerSpec.kt
autonomous: true
requirements:
  - REQ-language-profiles
estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-01/D-04: a fresh install has one enabled embedded en profile with distinct valid primary and active references."
    - "D-02: legacy current/custom history becomes one ordered snapshot exactly once, preserving the valid active layout."
    - "D-03: profile identity is immutable and distinct from mutable custom sourceUri."
    - "Selecting a valid profile persists one complete snapshot and swaps a fully loaded IME session without null state or input-view recreation."
  artifacts:
    - path: 8vim/src/main/kotlin/inc/flide/vim8/ime/language/LanguageProfile.kt
      provides: "LanguageProfile, LanguageConfig, profile source identity, and resolved session contracts"
      contains: "data class LanguageProfile"
    - path: 8vim/src/main/kotlin/inc/flide/vim8/ime/language/LanguageManager.kt
      provides: "Application-scoped sole writer and resolved-session publisher"
      contains: "class LanguageManager"
    - path: 8vim/src/main/kotlin/inc/flide/vim8/AppPrefs.kt
      provides: "One ordered JSON PreferenceData<LanguageConfig> snapshot"
      contains: "prefs_language_config"
  key_links:
    - from: 8vim/src/main/kotlin/inc/flide/vim8/Vim8ImeService.kt
      to: 8vim/src/main/kotlin/inc/flide/vim8/ime/language/LanguageManager.kt
      via: "observe resolved non-null session and assign KeyboardData inside the existing input view"
      pattern: "languageManager.*session"
    - from: 8vim/src/main/kotlin/inc/flide/vim8/ime/language/LanguageManager.kt
      to: 8vim/src/main/kotlin/inc/flide/vim8/AppPrefs.kt
      via: "normalize then call the single aggregate PreferenceData.set once"
      pattern: "languageConfig.*set"
---

<objective>
Establish the production-quality end-to-end language-state tracer: one ordered aggregate is persisted, owned by one application service, resolved to loaded keyboard data, and observed by the existing IME view.

Purpose: Prove the phase's hardest architectural constraint before settings and backup expansion, while implementing D-01, D-02, D-03, and D-04.
Output: Stable domain contracts, atomic persistence, idempotent migration, application-scoped manager, and load-then-swap IME observation.
</objective>

<execution_context>
@/home/unbot/code/opensource/9VIM/.codex/gsd-core/workflows/execute-plan.md
@/home/unbot/code/opensource/9VIM/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-multilingual-profiles-and-settings/02-CONTEXT.md
@.planning/phases/02-multilingual-profiles-and-settings/02-RESEARCH.md
@.planning/phases/02-multilingual-profiles-and-settings/02-PATTERNS.md
@.planning/phases/02-multilingual-profiles-and-settings/02-VALIDATION.md
@8vim/src/main/kotlin/inc/flide/vim8/AppPrefs.kt
@8vim/src/main/kotlin/inc/flide/vim8/VIM8Application.kt
@8vim/src/main/kotlin/inc/flide/vim8/Vim8ImeService.kt
@8vim/src/main/kotlin/inc/flide/vim8/ime/layout/Layout.kt
@8vim/src/main/kotlin/inc/flide/vim8/ime/layout/AvailableLayouts.kt
@8vim/src/main/kotlin/inc/flide/vim8/datastore/model/PreferenceData.kt

<interfaces>
Use `PreferenceData.set(value)` as the only durable aggregate write; `VIM8Application` owns lazy process-wide services exposed by `Context` accessors; layout loading remains `Either<LayoutError, KeyboardData>`. Preserve those concrete boundaries rather than introducing another preference model or test-only dependency seam.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one persisted active-profile selection into the live IME view</name>
  <files>8vim/src/main/kotlin/inc/flide/vim8/AppPrefs.kt, 8vim/src/main/kotlin/inc/flide/vim8/VIM8Application.kt, 8vim/src/main/kotlin/inc/flide/vim8/Vim8ImeService.kt, 8vim/src/main/kotlin/inc/flide/vim8/ime/language/LanguageProfile.kt, 8vim/src/main/kotlin/inc/flide/vim8/ime/language/LanguageManager.kt, 8vim/src/test/kotlin/inc/flide/vim8/ime/language/LanguageManagerSpec.kt</files>
  <read_first>02-CONTEXT.md D-01/D-03/D-04; AppPrefs.Layout; VIM8Application lazy accessors; Vim8ImeService current `prefs.layout.current.observe`; Layout.loadKeyboardData; LanguageManager patterns in 02-PATTERNS.md.</read_first>
  <behavior>
    - RED: a fresh aggregate resolves embedded en as enabled, primary, active, and last-valid per D-01/D-04.
    - RED: selecting a second valid profile loads it before mutation, performs one aggregate write, and emits exactly one non-null resolved session.
    - RED: a load failure leaves the persisted snapshot and current resolved session unchanged.
    - RED: the IME consumes the resolved KeyboardData without invoking `onCreateInputView` or clearing its current data.
  </behavior>
  <action>Write the focused LanguageManagerSpec cases first and run them to record an assertion/compile failure. Then add immutable `LanguageProfile.id`, a discriminated embedded/custom source with mutable custom `sourceUri`, ordered `LanguageConfig` fields for profiles/primary/active/last-valid, and `ResolvedLanguageSession`. Register exactly one exportable aggregate preference in the existing AppPrefs identity. Implement `LanguageManager.selectActive(profileId)` as load → validate non-zero-layer data → compute complete next snapshot → one preference write → one resolved-session publication. Construct one lazy manager in VIM8Application and expose `Context.languageManager()`. Replace the IME's raw current-layout observer with resolved-session observation, assigning KeyboardData inside the existing Compose input view. Keep legacy `prefs.layout.current`, `custom.history`, and `previousValid` as migration inputs, not competing writers. D-03 is a locked one-way identity decision already approved by the user, so record the reversibility rating without opening another decision checkpoint.</action>
  <reversibility rating="one-way">D-03 profile IDs become persisted references; changing their meaning later requires migration, but the locked decision already authorizes this door.</reversibility>
  <verify>
    <automated>test "$(java -version 2&gt;&amp;1 | sed -n '1s/.*version "\([0-9][0-9]*\).*/\1/p')" = 17 &amp;&amp; test -d "${ANDROID_HOME:?}/platforms/android-36" &amp;&amp; ./gradlew :8vim:testDebugUnitTest --tests 'inc.flide.vim8.ime.language.LanguageManagerSpec'</automated>
    <fails_when>Fails when the pinned Java 17/API 36 environment is absent, the RED test was not observed before implementation, selection mutates before validation, more than one snapshot write/session emission occurs, or the focused spec is not green.</fails_when>
  </verify>
  <acceptance_criteria>The fresh EN path and one alternate valid selection run end-to-end through aggregate preference, application manager, loaded session, and the existing IME view; failed load is non-mutating.</acceptance_criteria>
  <done>The tracer is committed with recorded RED then GREEN evidence and no second language authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract and harden ordered snapshot serde and normalization</name>
  <files>8vim/src/main/kotlin/inc/flide/vim8/ime/language/LanguageProfile.kt, 8vim/src/main/kotlin/inc/flide/vim8/ime/language/LanguageConfigSerDe.kt, 8vim/src/main/kotlin/inc/flide/vim8/ime/language/LanguageConfigNormalizer.kt, 8vim/src/main/kotlin/inc/flide/vim8/ime/language/LanguageManager.kt, 8vim/src/test/kotlin/inc/flide/vim8/ime/language/LanguageConfigSerDeSpec.kt</files>
  <read_first>PreferenceSerDe interface; LayoutSerDe discriminator pattern; research Patterns 1-4; D-01/D-03/D-04; LanguageManager tracer contracts from Task 1.</read_first>
  <behavior>
    - RED: versioned JSON round-trips ordered embedded/custom profiles, nullable locale, enabled flags, and all three references exactly.
    - RED: missing aggregate is distinguishable from malformed-present and unsupported-future schema.
    - RED: duplicate IDs/source URIs, invalid references, and empty enabled sets normalize deterministically to valid state without reordering surviving profiles.
    - RED: displayName/locale edits cannot change immutable ID, source identity, order, primary, or active state per D-03.
  </behavior>
  <action>Start with failing table/property cases, then extract the tracer contracts into the named domain/serde/policy files. Use the already installed Jackson Kotlin mapper and a versioned DTO; ordered arrays remain Lists. Make malformed-present state repair to embedded en and never signal legacy bootstrap. Make the pure normalizer return a complete valid value before persistence: unique IDs, unique live custom URI identity, non-empty enabled membership, and primary/active/last-valid references constrained to usable members. Keep fallible parsing explicit with existing Arrow/result patterns; add no dependency.</action>
  <reversibility rating="costly">The versioned aggregate schema is exported and later consumed by restore, but explicit schema versioning and tests keep changes migratable.</reversibility>
  <verify>
    <automated>test "$(java -version 2&gt;&amp;1 | sed -n '1s/.*version "\([0-9][0-9]*\).*/\1/p')" = 17 &amp;&amp; test -d "${ANDROID_HOME:?}/platforms/android-36" &amp;&amp; ./gradlew :8vim:testDebugUnitTest --tests 'inc.flide.vim8.ime.language.LanguageConfigSerDeSpec' --tests 'inc.flide.vim8.ime.language.LanguageManagerSpec'</automated>
    <fails_when>Fails on toolchain mismatch, loss of list order/nullable metadata, malformed-present legacy resurrection, invalid normalized references, or any focused test failure.</fails_when>
  </verify>
  <acceptance_criteria>One versioned JSON preference preserves the complete ordered state and repairs invalid values before any observer can see them.</acceptance_criteria>
  <done>Serde and normalizer are green under focused tests and LanguageManager persists only their valid aggregate output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Bootstrap legacy single-layout state idempotently</name>
  <files>8vim/src/main/kotlin/inc/flide/vim8/AppPrefs.kt, 8vim/src/main/kotlin/inc/flide/vim8/ime/language/LanguageMigration.kt, 8vim/src/main/kotlin/inc/flide/vim8/ime/language/LanguageManager.kt, 8vim/src/test/kotlin/inc/flide/vim8/ime/language/LanguageMigrationSpec.kt</files>
  <read_first>D-02/D-14; AppPrefs migration and postInitialize; PreferenceModel per-entry migration loop; Phase 1 AvailableLayouts stale-current behavior; 02-RESEARCH Pattern 5.</read_first>
  <behavior>
    - RED: no legacy data creates only embedded en.
    - RED: valid legacy embedded/custom current is active and primary; each valid history URI is enabled after it in deterministic canonical-URI order.
    - RED: stale inactive URI is pruned without changing active; stale active uses valid previous then embedded en per D-14.
    - RED: a present aggregate, including a malformed one repaired by serde, prevents a second bootstrap from legacy keys.
  </behavior>
  <action>Write the migration matrix first and confirm failure. Bump AppPrefs schema version while keeping legacy keys registered solely as compatibility inputs. Implement cross-key bootstrap outside `PreferenceModel.migrate()` because its entry-at-a-time contract cannot build the aggregate. Detect true aggregate-key absence before bootstrap, validate custom documents through the production layout loader, sort legacy StringSet URI values deterministically, normalize, and write once. D-02's one-way migration contract is locked and therefore needs no duplicate user checkpoint. Never delete the legacy keys during this phase; demote them from authority and prove repeated initialization cannot resurrect them.</action>
  <reversibility rating="one-way">D-02 fixes upgrade semantics for installed users; changing it later requires a second explicit migration.</reversibility>
  <verify>
    <automated>test "$(java -version 2&gt;&amp;1 | sed -n '1s/.*version "\([0-9][0-9]*\).*/\1/p')" = 17 &amp;&amp; test -d "${ANDROID_HOME:?}/platforms/android-36" &amp;&amp; ./gradlew :8vim:testDebugUnitTest --tests 'inc.flide.vim8.ime.language.LanguageMigrationSpec' --tests 'inc.flide.vim8.ime.language.LanguageConfigSerDeSpec'</automated>
    <fails_when>Fails if bootstrap is not idempotent, history order varies, a stale URI remains enabled, D-14 precedence changes, legacy state is resurrected, or focused tests fail.</fails_when>
  </verify>
  <acceptance_criteria>Fresh and upgraded installs each converge to one valid aggregate with the previous active layout/custom history preserved exactly under D-02/D-14.</acceptance_criteria>
  <done>Migration has RED/GREEN evidence, one-write bootstrap, and no post-bootstrap legacy authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| SharedPreferences JSON → domain | Local persisted bytes may be stale, malformed, duplicated, or from a newer schema. |
| Layout source → resolved IME session | Custom provider content is untrusted until loaded and validated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-01 | Tampering / DoS | LanguageConfigSerDe | high | mitigate | Version/type validation, duplicate rejection, deterministic safe repair, and tests distinguishing absent from malformed-present data. |
| T-02-02 | Tampering | LanguageMigration | high | mitigate | Aggregate-presence idempotence guard, validated custom inputs, deterministic ordering, and D-14 fallback tests. |
| T-02-03 | DoS | LanguageManager → Vim8ImeService | high | mitigate | Load and validate before the sole write; publish one non-null resolved session only after success. |
</threat_model>

<verification>
Run the three focused suites after each task. At plan completion run `./scripts/baseline-check.sh` inside the Phase 1 pinned Java 17/API 36/Build Tools 36.0.0 Docker environment; failure is any preflight mismatch, test/lint/ktlint/checkstyle/APK failure, torn snapshot observation, or IME null/recreation regression.
</verification>

<success_criteria>
- D-01 through D-04 and the migration portion of D-14 are executable, tested contracts.
- AppPrefs exposes one aggregate preference and legacy layout keys have no production writer.
- One application LanguageManager is the only profile-state writer.
- Vim8ImeService observes fully loaded sessions without input-view recreation.
- No Phase 3 switching/per-app memory or Phase 4 smart-text behavior is introduced.
</success_criteria>

## Artifacts this phase produces

- New files: `LanguageProfile.kt`, `LanguageConfigSerDe.kt`, `LanguageConfigNormalizer.kt`, `LanguageMigration.kt`, `LanguageManager.kt`, and their three named JVM specs.
- New types: `LanguageProfile`, `LanguageSource`, `LanguageConfig`, `ResolvedLanguageSession`, `LanguageConfigSerDe`, `LanguageConfigNormalizer`, `LanguageMigration`, `LanguageManager`.
- New fields/functions: `AppPrefs.language.config`, `VIM8Application.languageManager`, `Context.languageManager()`, and manager initialization/selection/session-observation APIs.
- No external API matrix or schema-push artifact: this plan uses only local SharedPreferences/layout-loader boundaries and adds no third-party API or database schema.

<output>
Create `.planning/phases/02-multilingual-profiles-and-settings/02-01-SUMMARY.md` when done.
</output>
