---
phase: 02-multilingual-profiles-and-settings
plan: "02"
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - 8vim/src/main/kotlin/inc/flide/vim8/ime/language/LanguageConfigNormalizer.kt
  - 8vim/src/main/kotlin/inc/flide/vim8/ime/language/LanguageManager.kt
  - 8vim/src/main/kotlin/inc/flide/vim8/ime/layout/AvailableLayouts.kt
  - 8vim/src/main/kotlin/inc/flide/vim8/app/settings/CustomLayoutImportAdapter.kt
  - 8vim/src/test/kotlin/inc/flide/vim8/ime/language/LanguageManagerSpec.kt
  - 8vim/src/test/kotlin/inc/flide/vim8/ime/layout/AvailableLayoutsSpec.kt
  - 8vim/src/test/kotlin/inc/flide/vim8/app/settings/CustomLayoutImportAdapterSpec.kt
autonomous: true
requirements:
  - REQ-language-profiles
  - REQ-language-settings
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-09: a valid custom import is enabled and active, and reimporting the same canonical URI reuses its ID."
    - "D-10/D-11/D-13: disabling/removing profiles updates active/primary atomically and cannot disable the final enabled profile."
    - "D-12: forgetting custom state releases held grants and derived cache/history but never changes the external document."
    - "D-14: broken inactive URI removal keeps active unchanged; broken active URI uses last-valid then embedded en."
  artifacts:
    - path: 8vim/src/main/kotlin/inc/flide/vim8/ime/language/LanguageManager.kt
      provides: "Complete command surface for import, enable/disable, reorder, primary, metadata, removal and restore"
      contains: "fun import"
    - path: 8vim/src/main/kotlin/inc/flide/vim8/app/settings/CustomLayoutImportAdapter.kt
      provides: "Read-only SAF grant lifecycle around one manager import"
      contains: "takePersistableUriPermission"
  key_links:
    - from: 8vim/src/main/kotlin/inc/flide/vim8/app/settings/CustomLayoutImportAdapter.kt
      to: 8vim/src/main/kotlin/inc/flide/vim8/ime/language/LanguageManager.kt
      via: "one typed import call after taking a read grant"
      pattern: "languageManager.*import"
    - from: 8vim/src/main/kotlin/inc/flide/vim8/ime/language/LanguageManager.kt
      to: 8vim/src/main/kotlin/inc/flide/vim8/ime/layout/AvailableLayouts.kt
      via: "manager-owned validation/cache cleanup only; no legacy preference writes"
      pattern: "loadKeyboardData|remove"
---

<objective>
Expand the proven tracer into the full invariant-preserving mutation and custom-import surface.

Purpose: Implement D-09 through D-14 behind the sole manager before any Compose control can issue these commands.
Output: Deterministic enable/disable/remove/fallback operations and leak-free SAF import/removal behavior.
</objective>

<execution_context>
@/home/unbot/code/opensource/9VIM/.codex/gsd-core/workflows/execute-plan.md
@/home/unbot/code/opensource/9VIM/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-multilingual-profiles-and-settings/02-CONTEXT.md
@.planning/phases/02-multilingual-profiles-and-settings/02-RESEARCH.md
@.planning/phases/02-multilingual-profiles-and-settings/02-PATTERNS.md
@.planning/phases/02-multilingual-profiles-and-settings/02-01-SUMMARY.md
@8vim/src/main/kotlin/inc/flide/vim8/ime/layout/AvailableLayouts.kt
@8vim/src/main/kotlin/inc/flide/vim8/app/settings/CustomLayoutImportAdapter.kt

<interfaces>
Consume `LanguageConfig`, `LanguageConfigNormalizer`, `ResolvedLanguageSession`, and the application-scoped `LanguageManager` from Plan 01. AvailableLayouts may remain a loader/cache registry, but it must no longer write legacy preference state.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete manager transitions and deterministic fallback</name>
  <files>8vim/src/main/kotlin/inc/flide/vim8/ime/language/LanguageConfigNormalizer.kt, 8vim/src/main/kotlin/inc/flide/vim8/ime/language/LanguageManager.kt, 8vim/src/main/kotlin/inc/flide/vim8/ime/layout/AvailableLayouts.kt, 8vim/src/test/kotlin/inc/flide/vim8/ime/language/LanguageManagerSpec.kt, 8vim/src/test/kotlin/inc/flide/vim8/ime/layout/AvailableLayoutsSpec.kt</files>
  <read_first>D-04 and D-10 through D-14; AvailableLayouts import/select/stale cleanup; Plan 01 LanguageManager and normalizer interfaces.</read_first>
  <behavior>
    - RED: reorder preserves exact order while leaving primary and active unchanged.
    - RED: disable active chooses valid primary; disabling active-primary promotes the next enabled profile in persisted order to primary and active.
    - RED: final-enabled disable is rejected without a write or session emission.
    - RED: broken inactive removes only that profile; broken active falls back last-valid then embedded en, never list-first.
    - RED: metadata edit changes only displayName/locale; remove custom clears profile/history/cache/grant after a valid state transition.
  </behavior>
  <action>Write the transition matrix before production edits and observe RED. Add explicit manager commands for enable/disable, reorder, setPrimary, metadata update, remove, and stale-source repair. Each command reads one snapshot, validates required sources, computes the entire next state with the pure policy, persists at most once, and publishes at most one loaded session. For D-10/D-11, apply operation-specific precedence rather than a general first-enabled fallback. For D-12, separate state commit from idempotent external cleanup and return cleanup failure explicitly without rolling back to dangling state. Refactor AvailableLayouts into manager-owned validation/cache helpers or remove its preference mutations while preserving Phase 1 cache/hash behavior.</action>
  <reversibility rating="costly">The command/fallback semantics will be consumed by settings, restore, and later switching; changing them requires coordinated callers but no irreversible data loss.</reversibility>
  <verify>
    <automated>test "$(java -version 2&gt;&amp;1 | sed -n '1s/.*version "\([0-9][0-9]*\).*/\1/p')" = 17 &amp;&amp; test -d "${ANDROID_HOME:?}/platforms/android-36" &amp;&amp; ./gradlew :8vim:testDebugUnitTest --tests 'inc.flide.vim8.ime.language.LanguageManagerSpec' --tests 'inc.flide.vim8.ime.layout.AvailableLayoutsSpec'</automated>
    <fails_when>Fails on toolchain mismatch, arbitrary-first fallback, torn primary/active/enabled state, a final-profile mutation, legacy preference writes, or focused test failure.</fails_when>
  </verify>
  <acceptance_criteria>Every manager command preserves order and membership invariants, emits only resolved sessions, and implements D-10 through D-14 exactly.</acceptance_criteria>
  <done>Manager mutation matrix passes with exactly-once write/emission assertions and Phase 1 layout regressions remain green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route custom SAF import and permission cleanup through the manager</name>
  <files>8vim/src/main/kotlin/inc/flide/vim8/app/settings/CustomLayoutImportAdapter.kt, 8vim/src/main/kotlin/inc/flide/vim8/ime/language/LanguageManager.kt, 8vim/src/test/kotlin/inc/flide/vim8/app/settings/CustomLayoutImportAdapterSpec.kt, 8vim/src/test/kotlin/inc/flide/vim8/ime/language/LanguageManagerSpec.kt</files>
  <read_first>D-03/D-09/D-12; Phase 1 CustomLayoutImportAdapter and spec; research Pitfalls 3, 4, and 6.</read_first>
  <behavior>
    - RED: successful live import derives/reuses identity from canonical URI, enables it, and makes it active.
    - RED: same URI reimport reuses one profile ID even after restore remapped that profile's backing URI.
    - RED: distinct URIs with identical bytes remain distinct live identities.
    - RED: rejected new URI releases only the newly acquired read grant and leaves state unchanged.
    - RED: removal releases a held grant/cache metadata and never invokes external document deletion.
  </behavior>
  <action>Start with failing adapter and manager tests. Keep `OpenDocument("*/*")` and read-only persistable permission semantics. Make the adapter track whether it acquired a grant, delegate exactly one typed import to the application manager, and release the new grant on validation failure. Implement D-09 URI-keyed upsert/activation while keeping D-03 immutable ID separate from mutable restored sourceUri. On D-12 removal, release permission only when held, clean manager-owned cache/history metadata, and never call provider delete/write. Preserve Arrow Either or the existing typed result at every fallible boundary.</action>
  <reversibility rating="reversible">The adapter remains a narrow Android boundary over the manager and can change locally without migrating persisted IDs.</reversibility>
  <verify>
    <automated>test "$(java -version 2&gt;&amp;1 | sed -n '1s/.*version "\([0-9][0-9]*\).*/\1/p')" = 17 &amp;&amp; test -d "${ANDROID_HOME:?}/platforms/android-36" &amp;&amp; ./gradlew :8vim:testDebugUnitTest --tests 'inc.flide.vim8.app.settings.CustomLayoutImportAdapterSpec' --tests 'inc.flide.vim8.ime.language.LanguageManagerSpec'</automated>
    <fails_when>Fails if a rejected grant leaks, import performs multiple manager mutations, same URI duplicates, restored identity is overwritten, external content is touched, or focused tests fail.</fails_when>
  </verify>
  <acceptance_criteria>Custom import/removal has one manager authority, exact D-03/D-09/D-12 identity semantics, and a balanced read-grant lifecycle.</acceptance_criteria>
  <done>Import and cleanup tests are green after recorded RED runs, with no new Android permissions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| SAF URI/provider → application | Provider identity, bytes, and persisted grant state are untrusted platform inputs. |
| UI/backup callers → LanguageManager | All callers may request transitions that would violate enabled/primary/active invariants. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-04 | Information disclosure / DoS | CustomLayoutImportAdapter | high | mitigate | Read-only grant only; release newly acquired grant on rejection; no broad storage/network permission. |
| T-02-05 | Tampering / DoS | LanguageManager transitions | high | mitigate | Pure operation-specific normalization, one aggregate commit, final-enabled rejection, and full fallback matrix tests. |
| T-02-06 | Tampering | Custom identity/cache cleanup | medium | mitigate | Canonical URI dedupe, immutable profile IDs, explicit cleanup ownership, and no provider delete/write calls. |
</threat_model>

<verification>
Run both focused task commands, then `./scripts/baseline-check.sh` in the Phase 1 pinned Java 17/API 36/Build Tools 36.0.0 Docker environment. Failure includes any Phase 1 custom-layout regression, leaked grant, invalid aggregate, lint/style/APK failure, or addition of network/storage permissions.
</verification>

<success_criteria>
- D-09 through D-14 are implemented by one manager and covered by focused RED/GREEN tests.
- AvailableLayouts no longer competes as a preference writer.
- Same canonical URI dedupes; same bytes at different URIs do not.
- No transition leaves zero enabled profiles or dangling active/primary references.
- No external custom document is deleted or modified.
</success_criteria>

## Artifacts this phase produces

- Expanded `LanguageManager` functions: import, enable/disable, reorder, setPrimary, updateMetadata, remove, and stale-source repair.
- Expanded `LanguageConfigNormalizer` operation policies for D-10/D-11/D-13/D-14.
- Updated `CustomLayoutImportAdapter` result/grant handling and manager delegation.
- Updated `AvailableLayouts` as manager-owned loading/cache support, plus focused regression cases in all three named specs.
- No external API matrix or schema push: all boundaries are Android SAF and local application state.

<output>
Create `.planning/phases/02-multilingual-profiles-and-settings/02-02-SUMMARY.md` when done.
</output>
